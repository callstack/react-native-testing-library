### Summary

Upgrade dependency versions for the root package, website, and Expo example apps while keeping the React Native testing matrix stable.

- Bumped root Jest/Babel/tooling packages to newer compatible releases.
- Updated root formatting, release, and typing-related dependencies, while keeping `@types/node` on the latest 24.x release.
- Upgraded website Rspress, React, and type packages to current React 19-compatible versions.
- Regenerated `examples/basic` from a fresh Expo blank TypeScript app, then restored the repo-specific sample code and tests on top of the new scaffold.
- Updated `examples/cookbook` to Expo `~54.0.33` and Expo Router `~6.0.23`.
- Confirmed `experiments-app` was already up to date for its current Expo SDK.

### Test plan

- `yarn lint`
- `yarn typecheck`
- `yarn test --watchman=false`
- `yarn build` (in `website/`)
- `yarn expo install --check` (in `examples/basic`)
- `yarn lint` (in `examples/basic`)
- `yarn typecheck` (in `examples/basic`)
- `yarn test --watchman=false` (in `examples/basic`)
- `yarn expo install --check` (in `examples/cookbook`)
- `yarn typecheck` (in `examples/cookbook`)
- `yarn test --watchman=false` (in `examples/cookbook`)
- `yarn expo install --check` (in `experiments-app`)

Validation gaps / notes:

- `yarn test` without `--watchman=false` fails in the local sandbox because Watchman cannot access its socket.
- `yarn install` at the root still reports an ESLint peer warning from the current `@callstack/eslint-config` dependency stack (`eslint-plugin-flowtype` and related packages request `^8.57.0` while the project uses ESLint 9.x).
- `yarn lint` in `examples/cookbook` fails with the existing config stack (`ConfigError: ... "parser": Expected object with parse() or parseForESLint() method`).
- `yarn typecheck` in `experiments-app` fails in untouched files [`src/utils/helpers.ts`](/Users/mdj/Development/OpenSource/react-native-testing-library/experiments-app/src/utils/helpers.ts:1) and [`src/screens/TextInputEvents.tsx`](/Users/mdj/Development/OpenSource/react-native-testing-library/experiments-app/src/screens/TextInputEvents.tsx:24).
